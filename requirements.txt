# Runtime dependencies, pinned so a clone (and CI) reproduces a known-good set.
# Bump deliberately: change a pin, run the suite, commit the pin with the result.
alpaca-py==0.43.5
fastapi==0.139.0
uvicorn[standard]==0.50.2
python-dotenv==1.2.2
tzdata==2026.2          # IANA timezone database for zoneinfo on Windows
anthropic==0.116.0      # research loop proposer (Claude API)
